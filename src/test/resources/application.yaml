spring:
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: validate
##    datasource:
##        url: ${POSTGRES_HOST:jdbc:postgresql://localhost:5431/db}
##        username: ${POSTGRES_USER:postgres}
##        password: ${POSTGRES_PASSWORD:postgres}
##        driverClassName: org.postgresql.Driver
    liquibase:
        enabled: true
        drop-first: false
        change-log: classpath:/db/changelog/${MIGRATION_LOG:changelog.xml}

#database:
#    driver:
#    url:
#    username:
#    password:

templates:
    thymeleaf:
        report: /templates/thymeleaf/office.rocker.html
        output: /templates/thymeleaf/output2.html
    freemarker:
        report: /templates/freemarker/office.ftl
        output: /templates/freemarker/output.html
    fizzed:
        report: /templates/fizzed/office.rocker.html
        output: /templates/fizzed/output.html
    mustache:
        report: /templates/mustache/office.html
        output: /templates/mustache/output.html
